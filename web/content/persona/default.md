# Persona overlay — default

This file mirrors the binding tone doc at `content/cbt_companion_tone_and_persona.md`.
Overlays in `age_band/` and `locale/` append short deltas on top of the default loaded by the app.

When authoring overlays, keep them **50–200 lines** and only override what differs.
