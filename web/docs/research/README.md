# Research manuscripts

LaTeX and supporting files for academic work related to Willow. **Not part of the Next.js runtime** — build with `make` inside each subfolder.

| Folder | Status |
|---|---|
| [`jmir-mental-health/`](./jmir-mental-health/) | Lit review / JMIR Mental Health framing |

LaTeX build artefacts (`*.aux`, `*.log`, …) are gitignored; run `make` locally to regenerate `main.pdf`.
