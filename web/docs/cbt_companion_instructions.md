# Moved

The runtime system prompt lives at [`content/cbt_companion_instructions.md`](../content/cbt_companion_instructions.md).

The draft in `docs/` was a duplicate; edit the file under `content/` only.
